class Solution:
    def isReachableAtTime(self, sx: int, sy: int, fx: int, fy: int, t: int) -> bool:
        d = max(abs(sx - fx), abs(sy - fy))
        if t < d:
            return False
        if d == 0 and t == 1:
            return False
        return True

# solution.py